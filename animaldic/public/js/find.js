document.addEventListener("DOMContentLoaded", () => {
  const find_form = document.querySelector();

  document.addEventListener("click", (e) => {
    const target = e.target;
    if (target.value === "아이디") {
      return res.render();
    }
    if (target.value === "비밀번호") {
      return res.render();
    }
    if (target.value === "다음") {
      return res.render();
    }
    return false;
  });
});
